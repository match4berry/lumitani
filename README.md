
  # github

  This is a code bundle for github. The original project is available at https://www.figma.com/design/jjGts7GIDWe7W9OTMwUacH/github.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  